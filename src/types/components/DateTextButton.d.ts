import "./ToolIcon.scss";
declare type LockIconProps = {
    title?: string;
    name?: string;
    onChange?(): void;
    isMobile?: boolean;
};
export declare const DateTextButton: (props: LockIconProps) => JSX.Element;
export {};
